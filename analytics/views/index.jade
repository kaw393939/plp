extends layout

block content
  script
    var socket = io.connect('http://localhost');
    socket.on('connect', function () {
      socket.send(window.location);
    });
  h1= title
  p Welcome to #{title}