extends layout

block content
  h4= title

  ul#dashboard

  script 
    var socket = io.connect('http://localhost:3000');
    socket.on('connect', function () {
      socket.on('dashboard', function (msg) {
        $('#dashboard').append('<li>' + msg.url + '</li>');
      });
    });
